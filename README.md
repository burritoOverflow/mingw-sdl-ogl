OpenGL interpolated triangle - built for MinGW

### Install prerequsites (built on Fedora 42):

```bash
sudo dnf install mingw64-gcc mingw64-SDL3 mingw64-SDL3-static wine
```

`Makefile` assumes this environment; see `SDL3_DIR`; adjust as needed for other development environments.
